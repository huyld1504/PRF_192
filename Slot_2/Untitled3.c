#include<stdio.h>

int main () {
	int a,b;
	
	printf("Nhap so a:");
	scanf("%d", &a);
	printf("Nhap so b:");
	scanf("%d", &b);
	
	int sum = a + b;
	printf("Tong 2 so a va b la: %d", sum);
	
	return 0;
}
